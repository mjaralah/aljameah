تغيير لون علامات التنقيط (dots) للوحدات الفرعية في الهيكل التنظيمي من `accent` (التيل) إلى `success` (الأخضر) في صفحة About العامة.

الملف المستهدف: `src/pages/About.tsx`  
الأسطر: 559 و 572  
التغيير: استبدال `bg-accent` بـ `bg-success` لعلامات الوحدات الفرعية فقط.